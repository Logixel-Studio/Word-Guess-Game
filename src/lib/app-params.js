/**
 * app-params.js — MIGRATED
 * Base44 app params removed. Supabase config is handled via .env.local.
 * This file is retained as a no-op to avoid import errors in any file that
 * still references it. It exports an empty appParams object.
 */
export const appParams = {};
